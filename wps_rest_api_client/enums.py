from enum import Enum


class EndpointsEnums(Enum):
    """ Enumerators used across endpoints.
    """
    PRODUCTS = "products"
    ATTRIBUTEKEYS = "attributekeys"
    ATTRIBUTEVALUES = "attributevalues"
    BLOCKS = "blocks"
    FEATURES = "features"
    IMAGES = "images"
    ITEMS = "items"
    RESOURCES = "resources"
    TAGS = "tags"
    TAXONOMYTERMS = "taxonomyterms"

    COUNTRY = "country"

    PRODUCT = "product"
    QUANTITIES = "quantities"